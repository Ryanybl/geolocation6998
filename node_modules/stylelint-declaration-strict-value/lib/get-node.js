'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _validation = require('./validation');

var getNode = function getNode(node) {
  var rawValue = node.toString().trim();

  // IMPORTANT detect vars for CSS, SCSS and Less
  if (_validation.reVar.test(rawValue)) {
    /* eslint-disable no-param-reassign */
    node.type = 'var';
    // make sure to mark walk through children as skipped
    node.skipChildren = true;

    return node;
  }

  return node;
};

exports.default = getNode;